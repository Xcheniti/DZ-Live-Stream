name: 📺 تحديث بث النهار TV
on:
  schedule:
    - cron: '0 */3 * * *' # تشغيل كل 3 ساعات
  workflow_dispatch: # تشغيل يدوي

permissions:
  contents: write

jobs:
  update-ennahar:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 استنساخ المستودع
        uses: actions/checkout@v4

      - name: 🐍 إعداد بايثون
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: 📦 تثبيت المكتبات
        run: pip install requests

      - name: 🚀 تشغيل القناص
        run: python ennahar_extractor.py

      - name: 🔄 رفع التحديثات إلى GitHub
        run: |
          git config --global user.name "Ennahar Bot"
          git config --global user.email "bot@ennahar.dz"
          git add results/*
          git diff --quiet && git diff --staged --quiet || (git commit -m "🔄 تحديث آلي لرابط النهار [$(date)]" && git push)
